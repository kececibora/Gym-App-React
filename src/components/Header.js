import Form from "./Form";

function Header() {
  return (
    <section className="todoapp">
      <header className="header">
        <h1>Fit Body</h1>
        <Form />
      </header>
    </section>
  );
}

export default Header;
